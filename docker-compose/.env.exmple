GEMINI_API_KEY=your_gemini_api_key_here
NODE_ENV=production

# error | warn | info | debug
LOG_LEVEL=info

# Song catalog database, stored in the named volume declared in docker-compose.yml
CATALOG_DB_PATH=/data/catalog.db
